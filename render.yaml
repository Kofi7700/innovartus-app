services:
  - type: web
    name: innovartus-saas
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    autoDeploy: true
    healthCheckPath: /health
